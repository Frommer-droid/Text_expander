# Text Expander v5.14.2 

Менеджер текстовых сниппетов для Windows. Позволяет быстро вставлять часто используемые фразы и шаблоны с помощью коротких аббревиатур.
  
## ✨ Возможности

- **Древовидная структура**: Удобная организация сниппетов по категориям.
- **Гибкое создание категорий**: Возможность создания подкатегорий, соседних и корневых категорий.
- **Фильтрация по окнам**: Привязка сниппетов к определённым приложениям по заголовку или классу окна.
- **Автозамена**: Мгновенная вставка текста при вводе аббревиатуры.
- **Поддержка Word**: Специальный режим для корректной работы в Microsoft Word.
- **Скан-коды аббревиатур**: Срабатывание по физическим клавишам, независимо от текущей раскладки.
- **Автозапуск**: Включение/выключение автозапуска из меню трея или на вкладке «Система».
- **Сворачивание в трей**: Приложение может работать в фоне, не занимая место на панели задач.
- **Управление окном**:
    - ЛКМ на `_` -> Свернуть.
    - ЛКМ на `X` -> Закрыть.
    - ПКМ на `X` -> Свернуть в трей.
    - Контекстное меню трея -> Выход.

## 🚀 Установка и запуск

1.  **Скачайте готовый установщик** (`Text_expander_vX.X.X_Setup.exe`) со страницы [Releases](../../releases) и следуйте инструкциям. Это полностью готовая к использованию программа.
2.  **Или запустите из исходного кода** (требуется Python 3.10+):
    1.  Установите зависимости:
        ```bash
        pip install -r requirements.txt
        ```
        *(Если файла requirements.txt нет, основные зависимости: `PySide6`, `pynput`, `pywin32`, `pyperclip`, `psutil`)*
    2.  Запустите приложение:
        ```bash
        python Text_expander.pyw
        ```

## 🛠 Разработка

Подробная информация для разработчиков находится в файле [DEVELOPER.md](DEVELOPER.md).
Номер версии хранится в `VERSION`, а история изменений — в [RELEASE_NOTES.md](RELEASE_NOTES.md).
